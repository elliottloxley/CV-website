const scrollObserver = {
    data() {
        return {
            viewPortObserverObject: {}
        }
    },
    methods: {
        // viewportObserve(onVisible, onNonVisible) {
        //
        // },
        // viewportObserveOnce(onVisible) {
        //
        // },
        // viewportObserveCustomOptions(onVisible, onNonVisible, options) {
        //
        // },
        // customRootObserve(root, onVisible, onNonVisible) {
        //
        // },
        // customRootObserveOnce(root, onVisible) {
        //
        // },
        // customRootObserveCustomOptions(root, onVisible, onNonVisible, options) {
        //
        // },
    }
}

export default scrollObserver;